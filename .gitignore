# Estratégia interna — brainstorms e plano estratégico, nunca publicáveis
# (contém nomes de pessoas, contatos e informações institucionais internas)
estrategia-interna/

# Rascunho da Rede da ARS (GE) — ainda não corrigido/unificado, não publicar
# até virar rede-ge.html de fato (ver ROADMAP.md §0 e Fase 9)
rede-ge-rascunho/

# Backup automatico do remontar_de_painel.py (o historico do git e o backup real)
painel.html.bak

# Token do GitHub usado pela tarefa agendada de revisao semanal (nunca versionar)
credenciais-github.env

# Saidas de build (geo.topojson, stats.json, municipios_dados*.json) — geradas
# por build_data.py/build_stats.py, pesadas e reproduziveis; nunca versionar
build/

# Cache do Python
__pycache__/
